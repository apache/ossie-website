# Specification

<!-- TODO: Re-implement build-time import of the OSI specification.
     The previous implementation used hooks/fetch_spec.py to download spec.yaml
     from GitHub and generate a virtual definitions page, plus
     mkdocs-include-markdown-plugin to inline spec.md. This was removed during
     cleanup. A new approach should be designed and implemented here. -->

This directory will contain the rendered OSI specification once build-time
import is re-implemented.
